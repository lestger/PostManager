import React from 'react';
import './loader.css'
const Loader = () => {
    return (
        <div className={'loader_box'}>
            <div className={'loader'}></div>
        </div>

    );
};

export default Loader;